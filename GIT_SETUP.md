# Инструкция по подключению к GitHub

## Текущий статус

✅ Git репозиторий уже инициализирован
✅ Все файлы созданы и готовы к коммиту

## Шаги для подключения к GitHub

### Шаг 1: Проверка текущего состояния

```bash
# Проверить статус
git status

# Проверить, есть ли уже подключенный remote
git remote -v
```

### Шаг 2: Добавление всех файлов

```bash
# Добавить все новые и измененные файлы
git add .

# Проверить, что все добавлено
git status
```

### Шаг 3: Создание первого коммита

```bash
# Создать коммит с описанием
git commit -m "Initial commit: Complete documentation structure for yacht production optimization

- Added documentation for all 15 process blocks
- Created optimization log and process flow documentation
- Added templates and changelog
- Updated README with full project structure"
```

### Шаг 4: Подключение к GitHub репозиторию

**Вариант A: Если репозиторий уже создан на GitHub**

```bash
# Замените USERNAME и REPO_NAME на ваши данные
# Например: https://github.com/username/yacht-production.git

git remote add origin https://github.com/USERNAME/REPO_NAME.git

# Или если используете SSH:
# git remote add origin git@github.com:USERNAME/REPO_NAME.git
```

**Вариант B: Если нужно создать новый репозиторий**

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите "+" в правом верхнем углу → "New repository"
3. Название: `yacht-production` (или другое)
4. Описание: "Yacht production optimization project"
5. Выберите Public или Private
6. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
7. Нажмите "Create repository"
8. Скопируйте URL репозитория
9. Выполните команду из Варианта A с вашим URL

### Шаг 5: Отправка на GitHub

```bash
# Убедитесь, что вы на ветке main
git branch -M main

# Отправить на GitHub
git push -u origin main
```

### Шаг 6: Проверка

1. Откройте ваш репозиторий на GitHub
2. Убедитесь, что все файлы загружены
3. Проверьте структуру папок

## Дальнейшая работа

### Ежедневный workflow

```bash
# 1. Проверить статус
git status

# 2. Добавить изменения
git add .

# 3. Закоммитить с описанием
git commit -m "Описание: что было изменено"

# 4. Отправить на GitHub
git push
```

### Примеры сообщений коммитов

```bash
# При добавлении оптимизации
git commit -m "Optimization: Improved resource allocation in block 5"

# При обновлении документации
git commit -m "Docs: Updated process description for block 3"

# При добавлении метрик
git commit -m "Metrics: Added KPI tracking for block 7"
```

## Решение проблем

### Если remote уже существует

```bash
# Проверить текущий remote
git remote -v

# Если нужно изменить URL
git remote set-url origin https://github.com/USERNAME/NEW_REPO_NAME.git
```

### Если нужно переименовать ветку

```bash
# Переименовать текущую ветку в main
git branch -M main
```

### Если возникли конфликты при push

```bash
# Получить последние изменения
git pull origin main

# Разрешить конфликты (если есть)
# Затем снова отправить
git push origin main
```

## Полезные команды

```bash
# Просмотр истории коммитов
git log

# Просмотр изменений в файле
git diff

# Просмотр статуса
git status

# Просмотр удаленных репозиториев
git remote -v

# Просмотр всех веток
git branch -a
```

## Готово! 🎉

После выполнения этих шагов ваш проект будет подключен к GitHub, и все изменения будут сохраняться в репозитории.
